<%= form_with(model: marca) do |form| %>
  <% if marca.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(marca.errors.count, "error") %> prohibited this marca from being saved:</h2>

      <ul>
        <% marca.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :codMar, style: "display: block" %>
    <%= form.number_field :codMar %>
  </div>

  <div>
    <%= form.label :dscMar, style: "display: block" %>
    <%= form.text_field :dscMar %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
